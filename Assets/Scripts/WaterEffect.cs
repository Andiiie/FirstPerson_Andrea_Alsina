using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class WaterEffect : MonoBehaviour
{
    
    Volume efecto;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if(efecto.profile.TryGet(out LensDistortion distortion))
        //{
        //    FloatParameter miVariable = new FloatParameter(Mathf.Cos(velocidad * Time.time));
        //    FloatParameter miVariable2 = new FloatParameter(Mathf.Sin(velocidad * Time.time));
        //    distortion.xMultiplier.SetValue(miVariable);
        //    distortion.yMultiplier.SetValue(miVariable2);
        //}
    }
}
